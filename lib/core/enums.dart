enum GameAction { cooperate, defect }

enum GameMode { vsComputer, vsHuman }

enum StrategyType {
  alwaysCooperate,
  alwaysDefect,
  titForTat,
  generousTitForTat,
  titForTwoTats,
  grudger,
  random,
  pavlov,
  suspicious,
  generous,
}

enum Winner { p1, p2, tie }

